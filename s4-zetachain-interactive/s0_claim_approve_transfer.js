
import { web3, wallet, wallets, pvt } from './index';
import { createTx } from './utils/createTx';
import { approve } from './utils/approve';

const gao = async (wallet, pvt) => {
  try {
    // 授权代币
    
  } catch (error) {
    console.log("failured in f3")
    console.log(error)
  }
} 